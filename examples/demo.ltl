// examples/demo.ltl — runnable demo for ltl_bloom_ds

import std::io
use ltl_bloom_ds as lib

fn main() {
    io::println("ltl_bloom_ds demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}